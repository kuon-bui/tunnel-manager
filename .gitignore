# Secrets and runtime state — never commit
data/*

# Local git worktrees
.worktrees/

# Subagent-driven-development scratch (task briefs, reports, review diffs —
# may contain locally-generated secrets, never committed)
.superpowers/

# Local env for backend workflows
.env
another-service.yml
